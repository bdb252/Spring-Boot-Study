<?xml version="1.0" encoding="UTF-8"?>

<!DOCTYPE mapper
	PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
	"http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.edu.springboot.mydiary.IMyDiary">

	<select id="getTotalCount" resultType="int"
	parameterType="com.edu.springboot.mydiary.MyDiaryDTO">
		SELECT COUNT(*) from diary
	</select>
	
	<select id="listPage"
		resultType="com.edu.springboot.mydiary.MyDiaryDTO"
		parameterType="com.edu.springboot.mydiary.ParameterDTO">
		SELECT * FROM (
		    SELECT tb.*, rownum rNum from (
		        SELECT * FROM diary	order by diary_idx desc
		    ) Tb
		)
		where rNum<![CDATA[>=]]>#{start}
				and rNum<![CDATA[<=]]>#{end}
	</select>
	<!-- where rNum between #{start}
				and #{end} -->
				
  	<insert id="write">
	 	insert into diary (diary_idx, member_idx, ofile, sfile, description, 
	 	temperature, humidity, sunlight)
	 	values
	 	(seq_board_num.nextval, #{_member_idx}, #{_ofile}, #{_sfile}, #{_description},
	 	#{_temperature}, #{_humidity}, #{_sunlight})
	</insert>
	 
 	<select id="view"
 		resultType="com.edu.springboot.mydiary.MyDiaryDTO"
		parameterType="com.edu.springboot.mydiary.ParameterDTO">
		SELECT * FROM diary WHERE diary_idx=#{diary_idx}
	</select>
	
	
 	<update id="edit"
		parameterType="com.edu.springboot.mydiary.ParameterDTO">
		UPDATE diary SET
		
		WHERE diary_idx=#{diary_idx}
	</update>
	 
  	<delete id="delete">
		DELETE FROM myboard WHERE idx=#{param1}
	</delete>
</mapper>